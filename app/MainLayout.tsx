import React from 'react';

export default function MainLayout({children}: {childern: React.ReactNode}) {
  // const pathname = usePathname();

  return (
    <>
      <div className={'flex justify-between'}></div>
    </>
  )
}